name: Validate HP Framework

on:
  push:
  pull_request:

jobs:
  validate:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Validate package structure
        shell: pwsh
        run: ./.github/scripts/Validate-Package.ps1
