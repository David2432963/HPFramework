# Startup Pipeline

Register awaitable application readiness work from a derived `RootLifetimeScope`. IDs must be unique; timeout and retry policy belong to the registration rather than the task body.
