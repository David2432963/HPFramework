# Mobile Diagnostics

Add `RuntimeDebugOverlay.prefab` only to an Editor/Development debug composition path and inject it from the root scope. The HUD samples immutable framework diagnostics snapshots at a throttled interval and renders unsupported metrics as `n/a`.
